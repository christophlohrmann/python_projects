# do not load the full db every time, instead fetch only the parts of data that are needed

# subtype should already define all variables except the goal type

# feature: delete last added entry

# reload the tables if the database is updated

# backup mechanism for the gains!
